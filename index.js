// We could put the ESLint configuration directly in this file. But by putting
// it in a standardized external file, we can also use it to test this package.
module.exports = require('./.eslintrc.json')
