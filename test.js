/* eslint-disable no-unused-vars */

// This file is used to test with `standardx` and `./.eslintrc.json`. Therefore,
// we ignore the fact that none of the variables are used.

const commaDangle = {
  a: 'a',
}

function noSpaceBeforeFunctionParen() {
}

const spaceBeforeFunctionParen = function () {
}
